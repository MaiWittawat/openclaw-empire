services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: claw-empire-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: claw_empire
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  # backend:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   container_name: claw-empire-backend
  #   environment:
  #     DATABASE_DSN: postgres://postgres:postgres@postgres:5432/clawempire?sslmode=disable
  #     SERVER_PORT: 8080
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   restart: unless-stopped

  # frontend:
  #   build:
  #     context: .
  #     dockerfile: frontend/web/Dockerfile
  #     args:
  #       VITE_API_URL: http://localhost:8080
  #   container_name: claw-empire-frontend
  #   ports:
  #     - "80:80"
  #   depends_on:
  #     - backend
  #   restart: unless-stopped

volumes:
  postgres_data:

networks:
  default:
    name: claw-empire-network
